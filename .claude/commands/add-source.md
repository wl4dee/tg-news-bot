---
description: Перевірити фід і додати його в config/sources.txt
argument-hint: <url або @канал> [рубрика] [вага 1-3]
allowed-tools: Bash, Read, Edit
---

Треба додати джерело: **$ARGUMENTS**

Джерела живуть у `config/sources.txt`, не в коді. Твоя робота — перевірити,
що джерело робоче, і **дописати рядок самостійно** у потрібну секцію файлу.

## 1. Визнач тип

- Починається з `@` або схоже на ім'я ТГ-каналу → `tg`
- URL → `rss`

## 2. Перевір, що воно живе

**Для `rss`:**

```bash
curl -sS -L -o /tmp/feed.xml -w "HTTP %{http_code}  %{size_download} bytes  %{content_type}\n" --max-time 20 "<URL>"
```

Далі переконайся, що це справді фід, а не сторінка помилки:

```bash
python -c "import feedparser,sys; d=feedparser.parse(open('/tmp/feed.xml','rb').read()); print('bozo:',d.bozo); print('entries:',len(d.entries)); print('title:',d.feed.get('title')); [print(' -',e.get('title','?')[:80]) for e in d.entries[:3]]"
```

Приймається тільки якщо: HTTP 200 **і** `entries > 0` **і** заголовки виглядають
як новини. `bozo: 1` саме по собі не вирок — багато робочих фідів невалідні
формально, дивись на `entries`.

**Для `tg`:**

```bash
curl -sS -L --max-time 20 "https://t.me/s/<канал>" | grep -c "tgme_widget_message "
```

Нуль означає, що канал приватний або не має вебпрев'ю — тоді його брати не можна,
потрібен Telethon (відкладено до v2). Так і скажи.

## 3. Рубрика і вага

Рубрика — одна з наявних у файлі: `крипта`, `ринки`, `техно`, `Україна`, `Київ`.
Нову рубрику вигадуй тільки якщо користувач попросив явно — і тоді одразу додай
для неї рядок `limit`, інакше вона обійде баланс рубрик.

Вага за шкалою з документа: `3` — першоджерело (регулятор, суд, біржа, сама компанія);
`2` — нормальне ЗМІ; `1` — сигнал, потрібне підтвердження.
Якщо вагу не назвали — запропонуй свою і поясни одним реченням чому.

## 4. Додай рядок у файл

Встав його `Edit`-ом у `config/sources.txt`, під відповідний заголовок-секцію
(`# ═══════ КРИПТА ═══════` тощо), поруч із однотипними джерелами:

```
rss | https://... | крипта | 2
```

Потім переконайся, що парсер бачить нове джерело:

```bash
python -c "import sys;sys.path.insert(0,'.');from bot import docs;c=docs.parse_sources(docs.load(docs.SOURCES_PATH,'','джерела'));print(len(c['sources']),'джерел')"
```

Скажи одним рядком, що додав і з якою вагою. Джерело в код не додаємо ніколи.

Якщо фід не пройшов перевірку — **нічого не додавай**, скажи прямо з кодом
відповіді, що саме не так.
